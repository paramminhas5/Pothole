import { CreateCampaignClient } from './CreateCampaignClient';

export const metadata = {
  title: 'Start a Campaign — Sahayata',
  description: 'Turn your issue into a coordinated campaign with a named target, deadline, and escalation path.',
};

export default function CreateCampaignPage() {
  return <CreateCampaignClient />;
}
