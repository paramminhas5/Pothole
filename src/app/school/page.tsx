import { SchoolClient } from './SchoolClient';

export const metadata = {
  title: 'Power School — Sahayata',
  description: 'Learn how power works. Prove you did the work. Build a portable civic track record.',
};

export default function SchoolPage() {
  return <SchoolClient />;
}
